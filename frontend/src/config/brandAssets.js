/** Brand logos in /public — use PNG assets (not legacy .svg). */
const base = import.meta.env.BASE_URL

export const WOMEN_OWNED_LOGO_SRC = `${base}Untitled-1.png`
export const CACHE_LOGO_SRC = `${base}cache.png`
